              Global
----------------------------------
( ) новые TODO
(*) в процессе (с указанием ника)
(+) выполнено, но в стадии тестирования/утверждения
(?) идея на обсуждение/уточнение
54 next
----------------------------------

51 (*) DM на newsFolder [mz]
       Хм.. Не смог разобраться с:
       - getFolders - всё нормально - по newsFolderDataMapper::getFolders() должен возвращаться массив newsFolder (итератор - что ты уже сделал) угу, но только получается что нужно таки в newsFolder сделать newsFolder::getFolders(), newsFolder::getItems() и передавать ссылку на newsFolderMapper и чтобы уже у него спрашивать то что надо (получается немного похоже на прошлые АР/ТМ)
       - getItems - посмотри на строку 43
       в newsFolderMapper. 
       Как там делать? newsMapper создавать, и тогда жесткая привязка $section? (разве не логично, что если фолдер "принадлежит" секции, то и объекты новостей также должны "принадлежать" этой же секции??) Щас какая-то каша получилась... - 2 метода в newsFolder, ссылку на newsFolderMapper в конструктор; 2 метода, делегирующие обязанности newsFolderMapper'у и получается всё красиво
       
       Вроде сделал...Даже посадил list на все это :) Единственное что с тестами как то некрасиво, то и то тестим...


53 (*) в news, newsFolder, newsMapper, newsFolderMapper методы расставить одинаково в парах файлов - чтобы присматриваться к наследованию, а то перемешаны аццки (и так вроде нормально)

52 ( ) Мапу в ДМ наверное нужно кешировать, есть мнение что именно изза чтения-парсинга xml получаем такие тормоза
       и прочитай коменты на 45-48 в newsMapper (и аналогичные же в newsFolderMapper) - что думаешь?

49 (+) методы getOne, getRow, getAll как в pear::db (найти где уже юзаются подобные выборки ручками и заменить)
       заменено только в одном месте, в остальных используются prepared запросы

45 ( ) Придумать как красивее всего сделать проверку на обновление несуществующих новостей/фолдеров в АР/ТМ

44 ( ) Подумать над возможностью/необходимостью модификации метода update в newsTM, чтобы получал не только $data, 
       но и $id - а то потом могут возникнуть грабли
       Помнишь что сид сказал, что должны быть свои методы для каждого поля (setTitle, setText...) т.е. update вообще
       переделать надо и тогда никаких граблей с $id не будет.

43 ( ) Методы create для newsFolder и news

 1 ( ) метод edit для новостей (переделать update?)

42 ( ) set*/get* у ТМ

41 (?) Валидаторы

35 ( ) Подумать насчёт url-генератора, ибо редирект который ты сделал после правки привязывается к секции, а это не гут

 3 ( ) JIP

16 ( ) session_start() сделать фильтром (в будущем) 

14 ( ) Вынос общих методов у MVC модулей в simple-класс (начат View)

 0 ( ) Внедрение PEAR::CACHE_LITE

23 ( ) написать какой нить модуль для работы с todo и changelog

 2 ( ) i18n интерфейса и контента

 6 ( ) в шаблонах подправить стили. сделать css

24 ( ) Сделать storage и sessionstorage для файлов и БД MySQL 

13 ( ) Обновить SimpleTest до 1.0.1alpha3

