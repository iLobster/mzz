              Global
----------------------------------
( ) новые TODO
(*) в процессе (с указанием ника)
(+) выполнено, но в стадии тестирования/утверждения
(?) идея на обсуждение/уточнение
45 next_autoincrement_value ;)
----------------------------------

44 ( ) Подумать над возможностью/необходимостью модификации метода update в newsTM, чтобы получал не только $data, но и $id - а то потом могут возникнуть грабли

43 ( ) Методы create для newsFolder и news

42 ( ) set*/get* у ТМ

41 ( ) Валидаторы???

40 (+) Array dataspace (Чего-то особых плюсов не видно) (а ты их увидишь очень скоро, обещаю ;). ps: сам думал, сегодня после разговора с сидом убедился что плюсы есть)

37 (*) Toolkit вместо registry. Уточнить реализацию.

36 (*) сделать следующий способ именования таблиц - <module>_<section> (news_news, news_software, news_hardware...) и в 
	запросах юзать $this->table, который генерится уже из текущей секции (берётся из метода $this->getSection() - в нём - из реквеста) 
	и из имени модуля ($this->getName() || $this->getModuleName).

39 (+) проверить/пофиксить циклические включения шаблонов сами в себя {* main... и модулей самих в себя {load в активном и в шаблоне
       Если я правильно понял, то сделал. Единственное что нельзя вставить в тот же main два раза какой-то модуль. Можно конечно сделать
       выброс исключения при например 20 вставлений. Либо делать более сложный алгоритм, но тогда это отразится на скорости...

35 ( ) Подумать насчёт url-генератора, ибо редирект который ты сделал после правки привязывается к секции, а это не гут

34 (*) Сделать newsFolder - подробнее что оно будет делать (частично говорили, сёдня додумал):
	- метод getItems - который будет возвращать список элементов нём - т.е. новостей, как сейчас уже есть
	- getFolders - будет возвращать список ПОДПАПОК в этой ПАПКЕ, будет возвращаться также массив объектов ПАПКА
	- в будущем будет реализовано имхо деревом (неважно каким алгоритмом) - сейчас просто сделать плоскую таблу id | name | title, с корневой папкой и несколькими вложенными (все что не корневая - вложенные - оно пока НЕ ПРИНЦИПИАЛЬНО)

31 (*) перенести массив из фактори в xml

 3 ( ) JIP

28 (*) Доделать внедрение AR/TM в новости, готово: все новости, одна новость. Проблема возникла с редактированием: нужна модель для генерации формы (или это надо тоже в view?) + избавиться от условия if(..save..)

 1 (*) метод edit для новостей (зависит от 0) [zerkms/ml]

27 (*) переписать с mysqli на pdo [zerkms/ml]

26 (*) Перестать использовать @access и @static в комментариях. То что есть - очистил.

17 (*) Переписать исключения (создать набор стандартных исключений)

18 (*) Сделать загрузку файлов из пачки fileLoader::load в одном файле. Например: fileLoader('exceptions/setup'); init.php / setup.php: <?php fileLoader::load('exceptions/mzzException'); ... ?> 

16 ( ) session_start() сделать фильтром (в будущем) 

14 ( ) Вынос общих методов у MVC модулей в simple-класс (начат View)

13 ( ) Обновить SimpleTest до 1.0.1alpha3

 0 ( ) Внедрение PEAR::CACHE_LITE

23 ( ) написать какой нить модуль для работы с todo и changelog

 2 ( ) i18n интерфейса и контента

 6 ( ) в шаблонах подправить стили. сделать css

24 ( ) Сделать storage и sessionstorage для файлов и БД MySQL 