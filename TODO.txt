308 next

------------------------------------0.0.8--------------------------------------

307 ( ) Разобраться с путями в генераторе: когда надо расширить модуль только в проекте,
        пути к проекту в генераторе нет, т.к. папки /<project>/modules/<module>/ не существует
        (+ проверить и исправить если создается пассивный шаблон когда экшн указывает на уже существующий контроллер)

306 (*) Forum
     +  1. Учитывать при move число постов в форуме
     +  2. Учитывать при move ссылку на последний форум

305 ( ) Serialize/Unserialize для форм

304 (+) ORM
     +  1. Сделать хук afterInsert / afterUpdate

303 (*) Меню:
     *  1. В шаблоне реализовать рекурсивный вывод вложенных пунктов (striker)
            страйкер: думаю Вам не понравится как я это дело сверстаю, так что жду от mz шаблон ;)
     +  2. Сверстать вывод вложенных пунктов меню

302 (*) Вёрстка:
    (+) 1. В js дерева для меню прописать пути до картинок нормально (mz)
     +  2. в menuItem.php нормально написать пути до картинок (striker)

300 (+) в simpleMapper::replaceRelated сделать проверку на то, что в значении находится объект (+ может проверять и instanceof?)

299 (+) Модуль user
     +  1. userOnline сделать не на ОРМ, а на обычных запросах (или подумать об исключении obj_id)
     +  2. сделать очистку старых userAuth, обновление последнего доступа, реализовать на обычных запросах (или подумать об исключении из ДО obj_id)
     +  3. После добавления пользователя в группу список не перегружается

298 (*) новости, страницы, файлменеджер
     *  1. оформить нормально перенос узлов (фолдеров, объектов)

296 (+) fileManager
     +  1. Убрать $path из folder::upload()

295 (+) ACL
     +  1. Реализовать новую идею, с переносом вызова ACL в ДО
     +  2. $access->import(array('obj_id', $this->getObjId())); - спрятать это куданить в accessMapper для удобства
           zerkms: впрочем - и так нормально ;)

294 (+) Голосование (striker)
     +  1. Сделать временные интервалы для голосований
     +  2. экшн viewActual

293 (*) Дерево (zerkms)
     +  1. Переделать все модули под новый класс
     +  2. Multi-tree
           zerkms: Jonix - пробуй переехать на новый класс
     +  3. Проверка на возможность переноса узла
     +  4. Как в старом дереве - убрать из имени имя рута
        5. Документация по классам

291 (+) Документация: (mz)
     +  1. нормально оформить деревья в примерах (например для 5.1 Описание структуры типичного модуля mzz)
     +  2. Глючит тег "example" (см. глава 3.10.1)

290 (?) ORM: сделать параметр lazy (zerkms)

289 (+) Перемещение элемента/ветки меню в другой парент (от js поступать будет id и новый parentId (mz)) (страйкер)

288 (+) url::add() вместо url::addParam() (mz)

279 ( ) Админка (zerkms)
     !  2. http://mzz/admin/view - убрать эту страницу, на её место поместить админку

277 ( ) Галерея (*)
     +  2. Сделать поддержку разных расширений (striker)
            страйкер: а поддержку разных библиотек для работы с изображениями будем делать?
            zerkms: сейчас - не вижу причин делать. потом - возможно.
     +  3. Тумбочки (разные типы картинок, см. 277.2) (striker)
     !  7. Вёрстка (mz)
     +  8. В "описание" фм закидывать имя/описание фотки в галерее
     +  9. При загрузке файлов с неверным расширением в галерею ошибки выводятся обычным списоком, а не как в fm (где-то не цепляется стиль)
     *  10.Придумать админку

256 (*) Каталог (*)
     *  1. подумать над непримитивными типами (типа img, file,...) (mz, striker)
     *  2. Интерфейс пользователя (mz, striker)
     *  3. В админке в динамических полях доделать подгружаемые селекты (striker)

------------------------------------later--------------------------------------

301 ( ) Переделать меню

297 ( ) Переезд конфига на рельсы каталога

227 (*) mzzForms (*)
     *  3. Свойство freeze для элементов (zerkms)
     ?  5. придумать где и как хранить оформление ошибок формы (?)
        6. Реализовать MAX_FILE_SIZE для form->file

280 (?) Сделать нормальный генератор хлебных крошек, чтобы можно было его добавить в news/view и другие экшны
            страйкер: генерировать массив url => title в контроллерах и скармливать breadcrumbs.tpl?

287 (?) шаблоны хранить в папке с модулем, при необходимости переопределять в www/templates

283 ( ) Система логгирования событий

282 ( ) mzzForms
        1. captcha
        2. аналог livesearch из пеара

263 ( ) Документация
        1. Подумать насчёт пунктов 4.1 и 4.2

248 ( ) Невалидния вёрстка в devToolbar

226 ( ) Задачи на devToolbar:
     ?  1. Вывод лога действий
            zerkms: я до сих пор не в курсе про этот пункт ;)
            mz: сделать простенькое сообщение вместо лога
            zerkms: например?
            mz: "Модуль forum создан" :)

     *  2. Работа со связями "класс"->"секция"
     +          a. вот тут у нас немножко нестыковочка - с одной стороны в БД хранятся именно указанные связи (класс-секция), с другой - только все классы конкретного модуля есть смысл привязывать к конкретной секции (что логично)
                    zerkms: состыковал всё ;)
                b.  далее - при "добавлении"/"удаления" класса в/из секцию(и) необходимо также модифицировать БД - создавать/удалять соотв. таблы. Это тема отдельного обсуждения в ирц
                    zerkms: пункт относится к инсталлеру/апдейтеру

        7. Возможно придётся во всех пунктах сделать галочку "не создавать файлы, модифицировать только БД"
                zerkms: даже если "да" (вероятнее всего - да), то это задача инсталлера/апдейтера, ибо в нормальных условиях такого функционала не должно потребоваться. По крайней мере я не могу придумать ситуации

        9. задокументировать интерфейс devToolbar, с картинками

     +  10.сделать редактор map-файлов (естественно с возможностью генерации по уже созданной таблице)

271 ( ) Инсталлятор/апдейтер

195 ( ) Авторегистрация классов. (http://mzz.ru/forum/viewtopic.php?id=74) (откладывается до момента реализации "инсталлятора модулей" или чего-то подобного)

 93 (?) Тестовая среда

135 (?) Class Date

 84 ( ) Проверить код на наличие комментариев с замечаниями и по возможности исправить или зафиксировать в TODO

 73 ( ) Начинать задумываться о мультиязычности контента и интерфейса

123 ( ) Подумать о целесообразности ведения истории версий объектов (новости/страницы сейчас)

214 (*) Убрать $GLOBALS из contentFilter && smarty_function_load
        (403level временно засуспенден ввиду необоснованности его создания ;) )
