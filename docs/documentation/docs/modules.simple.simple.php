<p><code>Simple</code> - это абстрактный класс, €вл€ющийс€ одной из двух составл€ющих <a href="structure.orm.html#structure.orm">ORM</a> в mzz. »менно от этого класса наследуютс€ все другие классы, €вл€ющимис€ доменными объектами бизнес-логики конкретного модул€.  ак правило в наследниках этого класса происходит переопределение двух методов: <code>getJip()</code> и <code>getAcl()</code>. ѕервый - реализует получение и отображение <a href="structure.jip.html"><code>JIP</code></a>, второй - отвечает за получение прав на объект. ѕример переопределени€ метода <code>getJip()</code>:</p>
<!-- php code 1 -->
<p>¬ вышеприведЄнном примере было несколько изменено поведение метода, а именно: вторым аргументом в <code>simple</code> происходит передача <code>id</code> объекта, а здесь - путь в древовидной структуре до него, <code>$this->getPath()</code>.</p>
<p>ќ методе <code>getAcl</code> можно почитать в <a href="structure.acl.html#structure.acl.getacl">соответствующей главе</a>.</p>