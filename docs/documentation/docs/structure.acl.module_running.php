<p>ACL автоматически проверяет на возможность запуска действия модуля из шаблона. В случае, если права на выполнение действия есть - происходит запуск модуля, если прав нет - показывается страница 403 с соответствующим сообщением для пользователя (либо произвольная, определённая программистом страница). Для управления поведением системы проверки прав используется параметр <code>403handle</code>. Он может принимать 3 значения:</p>
<ul style="list-style-type: decimal;">
    <li><code>auto</code> - значение по умолчанию. Означает, что за проверку прав на запуск действия и вывод ошибки ответственен сам фреймворк.</li>
    <li><code>manual</code> - проверка прав производится, однако действие запускается в любом случае. Полученное в результате проверки прав значение носит лишь информационный характер для программиста. На основе его программист уже должен сам определить - запускать действие или нет, вывод ошибки доступа также становится ответственностью программиста. Значение можно получить из <a href="structure.classes.html#structure.classes.request"><code>httpRequest</code>'а</a>, имя переменной - <code>access</code>, область видимости - <code>SC_PATH</code>.</li>
    <li><code>none</code> - проверка прав запуска действия не производится. Действие запускается в любом случае.</li>
</ul>
<p>Вторым параметром, имеющим отношение к системе проверки прав, является <code>403tpl</code>. В нём указывается путь до шаблона, который будет отображён, в случае если на запуск метода у текущего пользователя не хватает прав.</p>
<p>Пример запуска модулей:</p>
<<code smarty>>
{load module="news" action="list" section="news" 403handle="manual"}
{load module="news" action="list" section="news" 403tpl="news/deny.tpl"}
<</code>>
<p>В случае, если значение <code>403handle</code> не было изменено, и <code>403tpl</code> также не был указан - вызывается контроллер <a href="modules.simple.html#modules.simple.403controller"><code>simple403Controller</code></a>, реализация которого и определяет, что именно будет показано в случае, когда у пользователя не хватает прав на запуск действия. Реализация по умолчанию, идущая в фреймворке, представляет собой отображение страницы с именем <code>403</code> модуля <code>page</code>, секции <code>page</code>. В случае, если вам нужно переопределить логику работы этого контроллера - воспользуйтесь предусмотренным для этого <a href="structure.classes.html#structure.classes.resolver">механизмом переопределения классов</a>, используя резолверы.</p>