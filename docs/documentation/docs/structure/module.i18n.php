<p>В этой директории хранятся файлы локализации модуля (если она требуется). Файлы создаются отдельно для каждого из языков. Причём файлы должны именоваться следующим образом: <code>&lt;имя_языка&gt;.ini</code>. Например: <code>en.ini</code>.</p>
<p>todo ссылка на главу о i18n</p>