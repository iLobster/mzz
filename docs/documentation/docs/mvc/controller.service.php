<ul>
    <li>
        <code>simple404Controller</code>. Предназначен для вывода стандартной ошибки 404 "Запрашиваемый объект не найден".<br />
        Если вам нужно отобразить 404 ошибку у себя в контроллере - следует использовать примерно такой код:
<<code php>>
$controller = new simple404Controller();
return $controller->run();
<</code>>
        Также существует альтернативный способ передачи управления контроллеру 404 ошибки (todo ссылка на описание forward404):
<<code php>>
$mapper = $this->toolkit->getMapper('news', 'news');
...
$this->forward404($mapper);
<</code>>
        В этом случае будет произведён поиск контроллера <code>news404Controller</code> в директории <code>news/controllers</code>. И в случае, если он будет найден - то будет вызван именно он. В противном случае - также будет вызываться <code>simple404Controller</code>.<br />
        В качестве шаблона для этого контроллера используется файл <code>simple/404.tpl</code>, который вы, конечно же, можете переопределить у себя в проекте (<a href="extending.system.html#extending.system">подробнее</a>)<br />
        Также он автоматически вызывается для адресов, которые не были обработаны ни одним из роутов.
    </li>

    <li>
        <code>simple403Controller</code>. Предназначен для вывода стандартной ошибки 404 "Доступ запрещён".<br />
        Если вам нужно отобразить 403 ошибку у себя в контроллере - следует использовать примерно такой код:
<<code php>>
$controller = new simple403Controller();
return $controller->run();
<</code>>
        Также существует альтернативный способ передачи управления контроллеру 403 ошибки (todo ссылка на описание forward403):
<<code php>>
$mapper = $this->toolkit->getMapper('news', 'news');
...
$this->forward403($mapper);
<</code>>
        В этом случае будет произведён поиск контроллера <code>news403Controller</code> в директории <code>news/controllers</code>. И в случае, если он будет найден - то будет вызван именно он. В противном случае - также будет вызываться <code>simple403Controller</code>.<br />
        В качестве шаблона для этого контроллера используется файл <code>simple/403.tpl</code>, который вы, конечно же, можете переопределить у себя в проекте (<a href="extending.system.html#extending.system">подробнее</a>)<br />
        Также он автоматически вызывается для адресов, для которых в результате проверки прав достпупа был получен отказ (todo ссылка на роли).
    </li>
</ul>