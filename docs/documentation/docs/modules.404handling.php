<p>Перехват и обработка ошибки 404 производится mzz автоматически. В случае, если не найден запрашиваемый объект, будет запущен <code>simple404Controller</code>, в обязанности которого входит отображение сообщения об ошибке. Заменить стандартное сообщение, можно двумя путями:</p>
<p>1. Переопределение класса <code>simple404Controller</code>. Для этого следует в каталоге приложения (по умолчанию - <code>www</code>) создать подкаталог <code>simple</code>, а в этот каталог положить файл <code>simple404Controller.php</code>, в котором определить "заново" этот класс. Теперь встроенный механизм резолверов при запросе файла <code>simple404Controller.php</code> будет запрашивать только что созданный файл.</p>
<<note>>Последнее предложение можно считать справедливым лишь в случае, когда был использован композитный резолвер по умолчанию, либо его поведение сходно со стандартным (поиск файлов сначала в каталоге с проектом, затем - в системном каталоге).<</note>>
<<note>><strong>Не забывайте чистить кэш!</strong> Резолвер по умолчанию является кеширующим, и до того, как файл с кешем резолвера (<code>tmp/resolver.cache</code>) не будет удалён, по запросу файла <code>simple404Controller.php</code> будет резолвиться системный файл.<</note>>
<p>В этом случае будет изменено отображение 404 страницы для всего сайта.</p>
<p>2. Создание собственного класса-обработчика. Приведём пример для модуля меню:</p>
<!-- php code 1 -->
<p>Как видно из кода - этот контроллер только выводит заранее определённый шаблон. Однако в нём может быть запрограммирована сколь угодно сложная логика для обработки данной ситуации.</p>