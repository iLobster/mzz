<p>Для вставки адреса (URL) в шаблон можно использовать функцию <code>{url}</code>. Результатом работы этой функции является сгенерированный URL, который содержит:
</p>
<dl>
 <dd>- текущий протокол (HTTP/HTTPS);</dd>
 <dd>- адрес HTTP-хоста (HTTP_HOST);</dd>
 <dd>- порт сервера, если он отличается от стандартного "80";</dd>
 <dd>- дополнительный путь, который указан в конфигурационной константе SITE_PATH;</dd>
 <dd>- используемый язык, если включена мультиязычность в <a href="structure.classes.html#structure.classes.routers">Routes</a>;</dd>
 <dd>- секция;</dd>
 <dd>- параметры, если они были указаны;</dd>
 <dd>- действие.</dd>
</dl>

<p>Для получения текущего URL вызывается <code>{url}</code> без каких-либо аргументов.</p>

<p>Синтаксис функции:</p>
<<code smarty>>
{url module="модуль" section="секция" action="действие" route="имя"}
<</code>>
<p>Описание аргументов:</p>
<dl>
        <dd>- <em>module</em>: имя модуля, если секция не указана по нему будет определена секция;</dd>
        <dd>- <em>section</em>: имя секции, на которую будет ссылаться URL. Если не указана, будет использована текущая или, если указан модуль, секция модуля;</dd>
        <dd>- <em>action</em>: действие для указанного section;</dd>
        <dd>- <em>route</em>: имя правила для маршрутизации сборки URL.</dd>
</dl>
<p>Все аргументы являются не обязательными.</p>
<p>Функция может принимать так же и любые другие аргументы, которые будут являться параметрами.
Примером генерации http://example.com/news/4/asc/edit в соответствии с правилом маршрутизации <code>:section/:id/:sort/:action</code> является:
</p>
<<code smarty>>
{url section="news" action="edit" id="4" sort="asc" route="newsList"}
<</code>>

<p>Пример генерации URL для редактирования объекта с ID 4 в секции "news" (http://example.com/news/4/edit):</p>
<<code smarty>>
{url section="news" action="edit" id="4"}
<</code>>
<<note>>
Функция генерирует только URL. Например, сделать ее ссылкой можно следующим образом: <code>&lt;a href="{url}"&gt;link&lt;/a&gt;</code>
<</note>>

<p>Иногда возникает ситуация, когда требуется также передать и обычные GET параметры. Сделать это можно так:</p>
<<code html>>
<a href="{url _param="value" _param2="value2"}>link</a>
<</code>>
<p>Параметр <code>appendGet</code> определяет надо ли добавить в URL текущие GET-параметры или исключить их из URL.</p>